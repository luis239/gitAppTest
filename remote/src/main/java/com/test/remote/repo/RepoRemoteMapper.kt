package com.test.remote.repo

import com.test.data.model.ReposResponseData
//import com.test.remote.repo.model.GetReposResponse
import com.test.remote.repo.model.GetReposResponseItem


/*
fun GetReposResponse.map(){
getReposResponse.get

}

fun GetReposResponseItem.map(list: List<GetReposResponseItem>): List<ReposResponseData> {

    return list.map {
        ReposResponseData(it.id!!, it.name!!, it.updatedAt!!)
    }

}*/
